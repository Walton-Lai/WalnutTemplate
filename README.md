#include <bits/stdc++.h>

//DEDICATED TO Aaron Kenn Chandra
using namespace std;
// tip --- use "\n" instead of "endl" for cout as its faster

// common shortcut
using ll   = long long;
using pii  = pair<int, int>;
using pll  = pair<ll, ll>;
using vi   = vector<int>;
using vll  = vector<ll>;
using vpii = vector<pii>;
using vpll = vector<pll>;

// loop dee loopz
// go from i to n-1
#define range(i, n)      for (int i = 0; i < (n); ++i)
// go from a to b-1
#define FOR(i, a, b)   for (int i = (a); i < (b); ++i)
// go from b-1 to a (reversed)
#define ROF(i, a, b)   for (int i = (b) - 1; i >= (a); --i)
void solve() {
    // CODE GOES HERE
    
}

int main() {
    // input outputs are faster with this
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t= 1;
    cin >> t; //comment out if problem has only 1 test case
    while (t--) {
        solve();
    }
}
